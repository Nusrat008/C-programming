#include<stdio.h>
#include<math.h>



int main(){
    int x = 5;
    int *point;
    point = &x;
    printf("%d",point);

    // int a,b,c,x,y,z;
    // a = 9;
    // b =12;
    // c = 3;
    // x = a - b / 3 + c * 2 - 1;
    // y = a - b / (3+c) * (2-1);
    // z = a - (b / (3+c)*2) - 1;
    // printf("X= %d\nY = %d\nZ = %d\n",x,y,z);


    return 0;
}